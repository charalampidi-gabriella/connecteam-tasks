{
  "name": "connecteam-tasks",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "wrangler pages dev --proxy 5173 --port 8788 -- npm --workspace client run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "preview": "npm run build && wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
